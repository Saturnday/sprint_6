# Sprint_6 UI Autotests

Автотесты для проекта "Яндекс Самокат", написанные на Python + Selenium + Pytest + Allure. Используется Page Object Pattern.

## Установка зависимостей

```bash
pip install -r requirements.txt
